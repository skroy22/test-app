# It was already created through Ansible.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-app-pvc-argo-cephfs
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
#  volumeMode: block
  resources:
    requests:
      storage: 500Mi
  storageClassName: cephfs